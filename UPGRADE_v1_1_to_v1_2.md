# v1.1 → v1.2 更新内容

## 追加

- `/api/ask` エンドポイント
- 解析済みファイルへの追加質問UI
- 直近会話を使った連続質問
- 質問例ボタン
- 会話クリア
- Q&Aを解析JSONへ含める機能

## 方針

サーバーへソースコードを保持するセッションは作っていません。
追加質問のたびに、ブラウザが解析済みの元ファイルをlocalhostへ再送します。

## 依存関係

新しいPythonパッケージはありません。
既存の `.venv` をそのまま使えます。
