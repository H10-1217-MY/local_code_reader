# Local Code Reader v1.1

ソースコードを外部のLLMサービスへ送らず、ローカルのOllamaで「このファイルは何をしているか」を説明するためのローカルコード読解ツールです。

## v1.1でできること

- 1ファイルをブラウザから読み込み
- Pythonは `ast` で関数・メソッド・クラス・import・呼び出し先を静的解析
- Pythonの関数・クラスは開始行〜終了行を表示
- その他の言語は軽量なパターン解析
- 静的解析結果 + ソースコードをローカルOllamaへ渡して意味解析
- **静的解析で確認できた事実** と **LLMによる解釈** を画面上で分離
- LLMが主要関数を説明する際、静的解析結果と照合できれば行番号を併記
- 処理フローの二重番号を防止
- 役割、処理フロー、主要関数・クラス、入出力、依存、変更時の注意点、不明点を表示
- 解析結果だけをJSONとしてブラウザから保存
- multipart一時ファイルを避け、ソースコードはHTTP本文をメモリ上で処理
- アプリ側ではソースコードをファイル保存しない
- Conda環境が有効でも `run.sh` は必ずプロジェクト内 `.venv` のPythonを使用

## 対応拡張子

Python / C / C++ / JavaScript / TypeScript / Java / C# / Go / Rust / JSON / YAML / TOML / INI / Shell / PowerShell / SQL / Markdown / Text など。

Python以外の静的解析はv1.1では簡易版です。LLM側の意味解析と組み合わせて使います。

## 前提

- Python 3.11+ 推奨
- Ollamaがローカルで起動していること
- 使用するモデルが `ollama list` に存在すること

Ollamaが既に起動しているかは次で確認できます。

```bash
curl http://127.0.0.1:11434/api/tags
```

モデル例:

```bash
ollama list
```

必要なら:

```bash
ollama pull qwen3:8b
```

より丁寧なコード説明を優先する場合は、GPU/RAMに余裕があれば `qwen3:14b` などへ変更できます。

## セットアップ

```bash
cd local_code_reader_v1
./setup.sh
```

`setup.sh` は `.venv` を作り、依存関係をそこへインストールします。Condaの `(base)` が表示されていても問題ありません。

アプリ起動:

```bash
./run.sh
```

`run.sh` は現在の `python` コマンドではなく、必ず次を使用します。

```text
./.venv/bin/python
```

ブラウザで次を開きます。

```text
http://127.0.0.1:8000
```

## モデル変更

画面上でモデル名を変更できます。環境変数でデフォルトを変える場合:

```bash
export OLLAMA_MODEL=qwen3:14b
./run.sh
```

## v1.1での情報の扱い

画面を大きく2種類に分けています。

### 静的解析 / 確認できた事実

Pythonでは `ast` から直接取得した情報です。

- import
- 関数・メソッド名
- クラス名
- 開始行・終了行
- 引数
- 主な関数呼び出し

### Ollama / LLMによる解釈

コードの意味を読む必要がある情報です。

- ファイルの役割
- 処理フロー
- 各関数の役割
- 入出力の意味
- 変更時の注意
- 関連ファイル

LLMの説明中の関数名が静的解析結果と一致した場合は、画面上で行範囲を付けて表示します。一致しない名前は「静的解析で未照合」と表示します。

## データの流れ

```text
ブラウザでファイル選択
        ↓
FastAPI（生のHTTP本文をメモリ上で読込）
        ↓
静的解析 ──────→ 確認できた事実として表示
        ↓
localhost:11434 / Ollama
        ↓
構造化JSON ────→ LLMによる解釈として表示
        ↓
ブラウザ
```

このアプリ自身にはソースコードをディスク保存する処理を入れておらず、アップロードも multipart/UploadFile を避けて生のHTTP本文として読みます。ただし、OS、ブラウザ、プロキシ、ログ設定など別レイヤーの運用条件まで含めた「絶対に痕跡が残らない」という保証ではありません。機密コードで使う場合は `127.0.0.1` のまま運用し、外部公開しないでください。

## 次の予定

### v1.2: ファイルQ&A

- 「この関数は具体的に何をしている？」
- 「変更するならどこ？」
- 「この設定値は何に使われる？」

のように、解析した1ファイルについて追加質問できるようにする予定です。

### v2: フォルダ解析

```text
project/
  main.py
  detector.py
  config.yaml
      ↓
各ファイルを個別解析
      ↓
構造化インデックス
```

その後、依存関係・エントリーポイント・データフローを統合し、プロジェクト単位の引き継ぎ支援へ拡張します。
