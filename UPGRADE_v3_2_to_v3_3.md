# Upgrade: v3.2 → v3.3

v3.3はProject Q&Aの意味レベルのgroundingと、質問タイプ別の回答構成を強化する更新です。

## 1. AI解釈にも根拠factを必須化

v3.2では最終回答の固有名詞をFact Catalogで照合していました。v3.3では、AIによる短い結論と補足解釈を次の形式へ変更します。

```json
{
  "text": "...",
  "support_fact_ids": ["F012", "F018"]
}
```

有効な `support_fact_ids` がないsummary/解釈は回答に採用しません。

## 2. 具体的な意味上の飛躍を除外

選定済みプロジェクトに存在しない `.env`、設定ファイル名、コード風識別子などがAI解釈へ混ざった場合は除外します。

## 3. 静的解析の事実を追加

Pythonでは次を追加取得します。

- トップレベル定義名
- `os.getenv` / `os.environ` で参照する環境変数名
- 関数/メソッドのデコレータ
- 既存の関数内呼び出し候補をFact Catalogへ展開

値そのものは保存しません。

## 4. 静的構造からの役割推定

LLMのpurposeだけに頼らず、ファイル名、import、確認済みシンボル、言語、入口候補から保守的な `derived_role` を生成します。

## 5. 質問タイプ別テンプレート

特に以下を専用構成にしました。

- 言語別ファイル群の説明
- 引き継ぎ時の読む順番
- 依存関係
- 処理フロー
- 変更影響
- プロジェクト全体像

小規模プロジェクトの「読む順番」は入口候補だけで終わらず、依存グラフをたどって主要ファイル全体を並べます。

## 6. Fact Catalogの公平性

full Fact Catalogはサーバー側では先頭件数で切らず保持し、Ollamaへ送る候補だけをランキングして最大180件に制限します。これにより、関数数の多い巨大ファイルが後続ファイルのfactを押し出しません。

## 7. APIルートの静的fact化

Python関数デコレータから `app.get(...)` / `app.post(...)` 等を抽出し、APIルートを確認済みfactとして扱います。存在しないAPIパスをAIが補足した場合はsemantic groundingで除外できます。
